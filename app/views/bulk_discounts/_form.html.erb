<%= form_with url: path, method: method, local: true do |form|%>
  <h3><%= form.label label %>
  <p><%= form.label "Percentage Discount: " %></p>
  <p><%= form.text_field :percentage_discount, value: @bulk_discount.percentage_discount %></p>
  <p><%= form.label "Quantity Threshold: " %></p>
  <p><%= form.number_field :quantity_threshold, value: @bulk_discount.quantity_threshold %></p>
  <%= form.submit "Submit" %>
<% end %>
