<%= render partial: "form", locals: { path: merchant_bulk_discounts_path(@merchant),
                                      method: :post,
                                      label: "New Discount"
                                    } %>
