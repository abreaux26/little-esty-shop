<%= render partial: "form", locals: { path: merchant_bulk_discount_path(@merchant, @bulk_discount),
                                      method: :patch,
                                      label: "Edit"
                                    } %>
